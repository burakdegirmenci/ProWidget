# ================================
# ProWidget Admin Panel
# Separate deployment for Coolify
# ================================

version: '3.8'

services:
  admin:
    build:
      context: .
      dockerfile: Dockerfile.admin
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-https://widgetbackend.burakdegirmenci.me}
    container_name: pwx-admin
    restart: unless-stopped
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      PORT: 3001
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-https://widgetbackend.burakdegirmenci.me}
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=coolify"
      - "traefik.http.services.hoc8s8gsw44kwg4c8c8ws0gg-admin.loadbalancer.server.port=3001"
    networks:
      - coolify

networks:
  coolify:
    external: true
    name: coolify
